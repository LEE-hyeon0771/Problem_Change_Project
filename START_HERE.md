# 처음 실행하는 분께 (Windows)

개발을 몰라도 따라 할 수 있게 적었습니다. **순서대로만** 하시면 됩니다.
전부 합쳐서 처음 한 번은 15~20분 정도 걸리고, 그 다음부터는 30초면 켜집니다.

---

## 0단계 — 압축 풀기

받은 zip 파일을 **바탕화면**이나 **문서** 폴더에 풉니다.

> ⚠️ **다운로드 폴더나 압축 프로그램 안에서 바로 실행하면 안 됩니다.**
> 반드시 폴더로 풀어낸 뒤 진행해 주세요.
>
> ⚠️ 폴더 경로에 한글이나 공백이 많으면 문제가 생길 수 있습니다.
> `C:\exam-maker` 처럼 짧고 영문인 경로를 추천합니다.

---

## 1단계 — 프로그램 2개 설치하기

딱 두 개만 설치하면 됩니다. **둘 다 설치 화면에서 계속 "다음"만 누르면 됩니다.**

### (1) Node.js

1. https://nodejs.org/ 접속
2. 큰 초록 버튼 중 **LTS** 라고 적힌 쪽을 내려받기
3. 내려받은 파일 실행 → 계속 "Next" → "Install" → "Finish"

### (2) uv

1. 시작 메뉴에서 **PowerShell** 을 검색해서 엽니다.
2. 아래 한 줄을 그대로 복사해서 붙여넣고 엔터:

```powershell
powershell -ExecutionPolicy Bypass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

3. 설치가 끝나면 **PowerShell 창을 닫습니다.**

> 💡 `PowerShell requires an execution policy in [Unrestricted, RemoteSigned, Bypass]`
> 라는 빨간 오류가 나면, 위 명령에서 `-ExecutionPolicy Bypass` 부분이 빠진 것입니다.
> 위 줄을 **그대로** 복사해서 다시 실행해 주세요.
>
> 그래도 안 되면 아래를 먼저 실행하고(`Y` 입력), 위 명령을 다시 하면 됩니다.
>
> ```powershell
> Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

**설치 확인:** PowerShell 창을 새로 열고 `uv --version` 을 입력했을 때
버전 번호가 나오면 성공입니다.

> 💡 **설치 후에는 컴퓨터를 한 번 재시작해 주세요.**
> 재시작하지 않으면 다음 단계에서 "찾을 수 없습니다" 오류가 날 수 있습니다.

---

## 2단계 — API 키 넣기

이 프로그램은 구글 AI를 사용합니다. 사용하려면 **본인 키**가 필요합니다. (무료로 발급됩니다)

1. https://aistudio.google.com/apikey 접속
2. 구글 계정으로 로그인
3. **Create API key** 클릭
4. 만들어진 키를 복사 (`AIza...` 로 시작하는 긴 문자열)

그 다음:

5. 압축 푼 폴더에서 **`dev.bat`** 파일을 **더블클릭**합니다.
6. 검은 창이 뜨면서 `.env` 파일을 만들었다는 안내가 나오고 종료됩니다. (정상입니다)
7. 폴더 안에 새로 생긴 **`.env`** 파일을 **메모장**으로 엽니다.
   - 파일이 안 보이면: 탐색기 상단 `보기` → `표시` → `숨긴 항목` 체크
   - 여는 법: 파일 우클릭 → `연결 프로그램` → `메모장`
8. `GOOGLE_API_KEY=` 라고 적힌 줄을 찾아, **`=` 바로 뒤에** 복사한 키를 붙여넣습니다.

```
GOOGLE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

> ⚠️ 따옴표(`"`)나 띄어쓰기를 넣지 마세요. `=` 바로 뒤에 붙여야 합니다.

9. **저장**(Ctrl+S)하고 메모장을 닫습니다.

---

## 3단계 — 실행하기

**`dev.bat`** 을 더블클릭합니다.

- 처음 한 번은 필요한 파일을 내려받느라 **3~10분** 걸립니다. 검은 창을 닫지 말고 기다려 주세요.
- 준비가 끝나면 **브라우저가 자동으로 열립니다.**
- 안 열리면 브라우저 주소창에 직접 입력: **http://localhost:5174**

### 끄는 법

검은 창에서 **Ctrl+C** 를 누르거나 창을 닫으면 됩니다.

### 두 번째부터는

그냥 **`dev.bat` 더블클릭** 한 번이면 끝입니다. (30초 정도)

---

## 쓰는 법 간단 안내

첫 화면에서 세 가지 중 하나를 고릅니다.

| 버튼 | 무엇을 하나요 |
|---|---|
| **문제변형** | 영어 지문을 넣으면 모의고사 스타일 문제를 만들어 줍니다 |
| **핵심단어장** | 지문의 중요 단어를 뜻·품사·동의어·반의어까지 정리해 줍니다 |
| **개인DB** | 내가 저장한 문제와 단어장이 모이는 곳 |

### ⭐ 가장 중요한 것: "사용" 버튼

문제나 단어장을 만들면 화면에 결과가 나옵니다.
**아래쪽 "사용" 버튼을 눌러야 저장됩니다.**

누르지 않으면 저장되지 않습니다. 마음에 안 드는 결과는 그냥 다시 만들면 됩니다.

### 시험지 만들기

저장을 충분히 한 뒤 **모의고사 출제** 탭으로 갑니다.

- **문제 출제** → 저장한 문제로 시험지 + 해설지
- **단어 암기** → 저장한 단어장으로 암기장 또는 단어 시험지(정답지 포함)

만든 뒤 **`인쇄 / PDF 저장`** 을 누르고, 인쇄 창에서 대상을 **`PDF로 저장`** 으로 바꾸면 PDF가 됩니다.

---

## 문제가 생기면

### "uv 을(를) 찾을 수 없습니다" / "npm 을(를) 찾을 수 없습니다"

1단계 설치가 안 됐거나, 설치 후 재시작을 안 한 경우입니다.
**컴퓨터를 재시작**한 뒤 다시 `dev.bat` 을 실행해 보세요.

### uv 설치할 때 "execution policy" 빨간 오류

1단계 (2) 의 명령에서 `-ExecutionPolicy Bypass` 부분이 빠졌을 때 납니다.
아래를 **그대로** 복사해서 다시 실행하세요.

```powershell
powershell -ExecutionPolicy Bypass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

이 오류는 uv 를 설치할 때 한 번만 겪습니다. `dev.bat` 실행에는 영향이 없습니다.

### 검은 창이 순식간에 사라짐

`dev.bat` 을 더블클릭하지 말고, 폴더 안에서 빈 곳을 `Shift + 우클릭` →
`여기에 PowerShell 창 열기` 를 누른 뒤 아래를 입력하면 메시지를 볼 수 있습니다.

```powershell
.\dev.bat
```

### "포트 ... 를 이미 다른 프로그램이 쓰고 있습니다"

이 프로그램이 이미 켜져 있을 수 있습니다. 열려 있는 검은 창을 모두 닫거나,
컴퓨터를 재시작한 뒤 다시 실행해 주세요.

### 문제가 안 만들어지고 오류가 남

`.env` 파일의 API 키를 다시 확인해 주세요. 가장 흔한 원인입니다.

- `=` 뒤에 키가 제대로 붙어 있는지
- 따옴표나 앞뒤 공백이 들어가지 않았는지
- 키를 새로 넣었다면 `dev.bat` 을 껐다가 다시 실행했는지

### 그래도 안 되면

검은 창에 나온 **빨간 글씨를 그대로 캡처**해서 보내 주세요.

---

## 참고: 비용

구글 AI 사용료는 **키를 발급받은 본인 계정**으로 청구됩니다.
무료 한도가 있어 가볍게 쓰는 정도는 대체로 무료 범위 안입니다.

실제로 얼마나 썼는지는 `problem_log` 폴더 안의 파일에서 볼 수 있습니다.
(단어장 1회 기준 대략 0.02달러, 약 25원 수준입니다.)
